# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
        width: 100px

    status:
        label: Status
        relation: status
        select: name
        sortable: false

    plan_name:
        label: Plan
        relation: plan
        select: name
        sortable: false

    invoice_id:
        label: Invoice
        relation: invoice
        select: id
        sortable: false
        invisible: true

    original_period_start:
        label: Original Period Start
        type: date
        invisible: true

    original_period_end:
        label: Original Period End
        type: date
        invisible: true

    current_period_start:
        label: Current Period Start
        type: date
        invisible: true

    current_period_end:
        label: Current Period End
        type: date
        invisible: true

    next_assessment:
        label: Assessment
        type: date
        invisible: true

    activated_at:
        label: Activated On
        type: date
        invisible: true

    delay_activated_at:
        label: Activate On
        type: date
        invisible: true

    cancelled_at:
        label: Cancelled On
        type: date
        invisible: true

    delay_cancelled_at:
        label: Delay Cancelled
        type: date
        invisible: true

    expired_at:
        label: Expired On
        type: date
        invisible: true
