# ===================================
#  Form Field Definitions
# ===================================

fields:

    section_user:
        label: User details
        type: section

    user[name]:
        label: Name

    user[email]:
        label: Email

    user[company]:
        label: Company

    user[phone]:
        label: Phone

    section_billing:
        label: Billing details
        type: section

    # payment_method:
    #     label: Payment method

    user[street_addr]:
        label: Street address

    user[city]:
        label: City
        span: auto

    user[state][name]:
        label: State
        span: auto

    user[zip]:
        label: Zip / Postcode
        span: auto

    user[country][name]:
        label: Country
        span: auto
